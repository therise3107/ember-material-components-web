import mdcRippleAdapter from 'dummy/utils/mdc-ripple-adapter';
import { module, test } from 'qunit';

module('Unit | Utility | mdc ripple adapter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mdcRippleAdapter();
  assert.ok(result);
});
