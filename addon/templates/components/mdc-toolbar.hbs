{{yield (hash
  row=(component "mdc-toolbar/row" title-style=titleStyle first-row-style=firstRowStyle register-row=(action "registerRow") deregister-row=(action "deregisterRow") first-row=firstRow)
)}}
